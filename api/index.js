let menuData = null; let leadsData = null; try { menuData = require('../data/menu.json'); } catch(e) { menuData = [{id:'item-101',name:'Dal Papillon Royal',category:'today-special',price:'%E2%82%B4425',desc:'Our signature black lentils slow-cooked overnight with smoked tomatoes, white butter, and saffron cream.',tags:['Chef Special','Jain Option'],spice:1,isJain:true,isPopular:true,image:'assets/images/hero.png'}]; } try { leadsData = require('../data/leads.json'); } catch(e) { leadsData = [{id:'PAPILLON-7821',type:'Table Reservation',name:'Rajesh Sharma',phone:'+91 98765 43210',email:'rajesh.sharma@example.com',date:'2026-08-08',time:'08:00 PM',guests:4,area:'Main Dining Room',tableNumber:'T-04 (Booth)',notes:'Anniversary celebration, needs Jain food options',status:'Confirmed',createdAt:'2026-08-05 14:30'}]; } module.exports = (req, res) => { try { res.setHeader('Access-Control-Allow-Origin', '*'); res.setHeader('Access-Control-Allow-Methods', 'GET, POST, PUT, DELETE, OPTIONS'); res.setHeader('Access-Control-Allow-Headers', 'Content-Type'); if (req.method === 'OPTIONS') { res.writeHead(204); res.end(); return; } const reqUrl = req.url || ''; const pathname = reqUrl.split('?')[0]; if (pathname === '/api/menu' || pathname === '/api/menu/') { if (req.method === 'GET') { res.writeHead(200, { 'Content-Type': 'application/json' }); res.end(JSON.stringify(menuData)); return; } } if (pathname === '/api/leads' || pathname === '/api/leads/') { if (req.method === 'GET') { res.writeHead(200, { 'Content-Type': 'application/json' }); res.end(JSON.stringify(leadsData)); return; } } res.writeHead(404, { 'Content-Type': 'application/json' }); res.end(JSON.stringify({error: 'API Endpoint Not Found'})); } catch(err) { res.writeHead(500, { 'Content-Type': 'application/json' }); res.end(JSON.stringify({error: 'Internal Server Error', details: err ? err.message : 'Unknown'})); } };